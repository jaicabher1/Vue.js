// Template Strings o Concatenacion

const client = "Juan"
const balance = 400

console.log(`El cliente: ${client} tiene un saldo de ${balance}`)

const product = {
    name : "Tablet",
    price : 300,
    available : true
}

console.log(`El producto: ${product.name} tiene un precio de: ${product.price} `)
